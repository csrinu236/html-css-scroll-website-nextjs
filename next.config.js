/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: 'build',
  reactStrictMode: true,
  // images: {
  //   loader: 'custom',
  //   loaderFile: './public/imageLoader.js', // <= Location of image loader
  // },
};

module.exports = nextConfig;
